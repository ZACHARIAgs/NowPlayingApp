<Window x:Class="NowPlayingApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Now Playing" Height="300" Width="600"
        Background="Black"
        WindowStartupLocation="CenterScreen"
        PreviewKeyDown="Window_PreviewKeyDown"
        SizeChanged="Window_SizeChanged">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" CornerRadius="0" GlassFrameThickness="-1" ResizeBorderThickness="6" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="CaptionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#33FFFFFF"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource CaptionButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <!-- Pre-blurred background image rendered on a perfectly scaled Canvas -->
        <Canvas x:Name="BackgroundCanvas" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False">
            <Image x:Name="BackgroundImage" Stretch="Fill" RenderTransformOrigin="0.5,0.5" RenderOptions.BitmapScalingMode="HighQuality">
                <Image.RenderTransform>
                    <RotateTransform x:Name="BackgroundRotate"/>
                </Image.RenderTransform>
            </Image>
        </Canvas>
        
        <!-- Darkening overlay -->
        <Rectangle Fill="#B0000000" />
        
        <!-- Content in the bottom left -->
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Width="110" Height="110" CornerRadius="12" ClipToBounds="True" VerticalAlignment="Bottom" Background="#33000000">
                <Image x:Name="AlbumArtImage" Stretch="UniformToFill" />
            </Border>
            
            <Grid Grid.Column="1" Margin="25,0,0,0" VerticalAlignment="Center" SizeChanged="Containers_SizeChanged">
                <Grid.RowDefinitions>
                    <RowDefinition Height="45"/>
                    <RowDefinition Height="35"/>
                </Grid.RowDefinitions>
                
                <Canvas x:Name="TitleCanvas" Grid.Row="0" ClipToBounds="True">
                    <TextBlock x:Name="TitleText1" Foreground="White" FontSize="32" FontWeight="Bold" Canvas.Top="0" Canvas.Left="0"/>
                    <TextBlock x:Name="TitleText2" Foreground="White" FontSize="32" FontWeight="Bold" Canvas.Top="0" Canvas.Left="0" Visibility="Hidden"/>
                </Canvas>
                
                <Canvas x:Name="ArtistCanvas" Grid.Row="1" ClipToBounds="True" Margin="0,5,0,0">
                    <TextBlock x:Name="ArtistText1" Foreground="#DDDDDD" FontSize="20" Canvas.Top="0" Canvas.Left="0"/>
                    <TextBlock x:Name="ArtistText2" Foreground="#DDDDDD" FontSize="20" Canvas.Top="0" Canvas.Left="0" Visibility="Hidden"/>
                </Canvas>
            </Grid>
        </Grid>
        
        <!-- Custom Caption Buttons drawn over the client area -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" WindowChrome.IsHitTestVisibleInChrome="True" Background="Transparent">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Opacity" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <Button Content="&#xE921;" FontFamily="Segoe MDL2 Assets" FontSize="10" Style="{StaticResource CaptionButtonStyle}" Click="MinimizeButton_Click"/>
            <Button Content="&#xE922;" FontFamily="Segoe MDL2 Assets" FontSize="10" Style="{StaticResource CaptionButtonStyle}" Click="MaximizeButton_Click"/>
            <Button Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10" Style="{StaticResource CloseButtonStyle}" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
